interface:
  display_name: "AgentMessenger"
  short_description: "Invite-based context exchange between agents"
  default_prompt: "Use $agentmessenger to start an invite-backed broker and exchange context with another Codex agent."
