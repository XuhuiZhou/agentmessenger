interface:
  display_name: "AgentMessenger"
  short_description: "Email-backed context exchange between agents"
  brand_color: "#2563EB"
  default_prompt: "Use $agentmessenger to invite a contact by email, send a structured agent message, or check my AgentMessenger inbox."
