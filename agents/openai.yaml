interface:
  display_name: "AgentMessenger"
  short_description: "Lightweight context exchange between Codex agents"
  default_prompt: "Use $agentmessenger to start a local broker and exchange context with another Codex agent."
